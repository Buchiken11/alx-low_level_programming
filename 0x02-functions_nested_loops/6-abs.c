#include "main.h"
/**
 * _abs-computes and prints the absolute value of an intrger
 * @n: int n is here
 * Return: the integer checked
 */
int _abs(int n)
{

	if (n >= 1)
	{
		return (n);
	}
		return (-n);
}
