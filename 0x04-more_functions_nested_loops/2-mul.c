#include "main.h"

/**
 * mul-function multiply the two intrgers and return output
 * @a: a is variable 1
 * @b: b is variabl 2
 * Return: result(a*b)
 */

int mul(int a, int b)
{
	int mul =(a*b);
	return (mul);
}

