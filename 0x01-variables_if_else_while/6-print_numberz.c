#include <stdio.h>
/**
 * main-Entry point Afunction that prints base 10 numbers 0-10
 *
 * Return: Always 0. (Success)
 */
int main(void)
{
	int p;

	for (p = 48; p <= 57; p++)
	{
		putchar(p);
	}
		putchar('\n');
		return (0);
}
